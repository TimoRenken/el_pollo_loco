class CollidableObject extends MovableObject {

    /**
     * @type {object} - Numerical offsets used for collision check.
     */
    offset = {
        top: 0,
        right: 0,
        bottom: 0,
        left: 0
    };
}